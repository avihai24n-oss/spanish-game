# Realtime duel server (Cloudflare Worker) — used by production builds.
# Not a secret: the host is public by design; rooms are just random codes.
VITE_REALTIME_HOST=vamos-quiz-server.vamos-avihai.workers.dev
